# Decisions taken during execution

Eleven rulings made while executing
`docs/superpowers/plans/2026-09-01-ledge-name-patterns.md` without stopping to
ask. Each says what was decided, why, and what it costs if it was wrong. They
are here rather than in a scratch file because a decision taken on someone's
behalf that they never see was made in secret.

The plan and the spec are the argument; this is where the argument was
amended mid-flight.

## Ruling 1

The `noExtensions` → `noConditions` diagnostic change moves from Task 5
into Task 4, ahead of the defaults gaining `Screenshots`. — `RuleEditingTests.swift:244`
asserts `RuleSet.defaults.problems.isEmpty`, and a pattern-only default category
trips the old diagnostic, so Task 4 as written ends red and the plan's own rule
that every task ends green is broken. Moving the change is smaller than merging
the two tasks and keeps each commit bisectable. — If wrong, Task 4's diff is
larger than a reviewer expects for "add a category", and the two concerns are
reviewed as one gate instead of two.

## Ruling 2

Task 4 also rewrites the existing `RuleEditingTests.swift` expectation
`.noExtensions(category: empty.id)` to `.noConditions(...)`, and the two new
diagnostic tests move from Task 5 to Task 4 with it. — A renamed enum case leaves
that test uncompilable; the plan named neither the existing test nor its file.
Editing an existing test is normally forbidden without a ruling, so this is the
ruling: the test's subject is unchanged, only the case's name. — If wrong, an
existing assertion was weakened rather than renamed; the reviewer sees the
before/after in the diff.

## Ruling 3

The plan's mutation row "delete the trailing-star while loop → must fail
starMatchesNothingAtAll AND aLoneStarMatchesEverythingIncludingNothing" over-specified.
Only the second fails, and correctly so: `report*.pdf` resolves its star by mid-loop
backtracking and never reaches the cleanup line, which matters only for the `*`/""
case. Traced independently by the controller, agrees with the implementer.
Accepted as a plan defect, not an implementation or coverage gap — the row's real
requirement (something must fail) is met. — If wrong, a line of Glob.swift is
covered by one test rather than two.

## Ruling 4

The plan's row "drop the `!extensions.isEmpty` guard → fails
patternOnlyCategoryIgnoresTheExtension" under-predicted; it also fails
anyOneOfSeveralPatternsIsEnough, because both tests use `extensions: []`.
Over-coverage, not under-coverage — the row's requirement is met and then some.
No change. — If wrong, nothing: no line went uncovered.

## Ruling 5

The plan's mutation row "delete `result.appliedMigrations = ...` → fails
migrationRecordsItsMarker AND migrationIsANoOpOnceTheMarkerIsRecorded"
over-predicted. The no-op test returns through the early guard and never reaches
that line. Plan defect, no coverage gap — the row's requirement is met by the
first test. — If wrong, nothing went uncovered.

## Ruling 6

The implementer renamed a SECOND existing expectation
(.noExtensions → .noConditions in problemsAreOrderedByCategoryWithTheFallbackLast)
that the brief did not authorise. Accepted. — The enum case is renamed, so the
package does not compile with the old spelling; there was no path that both
honoured the one-edit limit and produced a building commit. The edit must be
purely mechanical, and the reviewer is being asked to confirm that specifically.
— If wrong, an existing assertion was weakened under cover of a rename, which
the diff shows plainly.

## Ruling 7

The plan's Step 1 code block for RuleSetMigrationTests.swift carried a
stray `}` — introduced when the controller moved the two diagnostic tests from

## Ruling 8

The plan's row "split on whitespace too → fails 2 tests" under-counted;
it fails 5, because most of the fixtures contain an internal space. Over-coverage.
No change. — If wrong, nothing went uncovered.

## Ruling 9

`parseNamePatterns` sits after `normalizedExtension` rather than
immediately after `parseExtensions`, to keep that pair adjacent. Accepted — the
brief said "after parseExtensions", which this satisfies, and splitting a
function from its helper to honour a literal reading would be worse code.
— If wrong, a reader looks two functions further down.

## Ruling 10

Real, and it enters the fix loop rather than being parked — the
sibling field was explicitly patched for this exact class, so leaving it open
makes two fields in one view behave differently for no reason a user could
guess. The brief's Step 3 omitted the call: a plan gap, not an implementer
error, and the implementer disclosed it rather than freelancing. — If wrong,
one round of fix and re-review is spent on a cosmetic resync.

## Ruling 11

The final whole-branch review covers 8883c41..HEAD — this plan's work
— not `git merge-base main HEAD`. The branch `implement-ledge` carries ~77
earlier commits building the app itself, which had their own final whole-branch
review in a previous session; re-reviewing them would bury this feature's diff in
one twenty times its size. — If wrong, an interaction between this feature and
older branch code goes unexamined; the task reviews did read the surrounding
files, which is partial cover.

